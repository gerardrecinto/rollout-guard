name: ci

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.9", "3.12"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - run: pip install pytest
      - run: pytest -q
      - name: demo run against failing window
        run: |
          pip install .
          rollout-guard check --metrics examples/metrics_failing.json \
            --rules examples/rules.json --deployment api --namespace prod || true
